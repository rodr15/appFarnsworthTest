import 'package:anthony/widgets/tool_bar.dart';
import 'package:flutter/material.dart';

class MenuFifteenChips extends StatefulWidget {
  const MenuFifteenChips({Key? key}) : super(key: key);

  @override
  _MenuFifteenChipsState createState() => _MenuFifteenChipsState();
}

class _MenuFifteenChipsState extends State<MenuFifteenChips> {
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        //ToolBar(),
      ],
    );
  }
}
